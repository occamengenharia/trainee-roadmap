# Fake API

Para o projeto em React iremos utilizar uma API fake.

```bash
  # Para iniciar o servidor 
  npx json-server server.json --port 3333
```

Agora basta acessar "http://localhost:3333/projects" que teremos todos os métodos HTTP (GET, POST, DELETE, PUT),   para listar, criar, deletar, atualizar um projeto.